# 🗓️ Meeting Planner — AI Calendar Assistant

A conversational meeting scheduler using **MCP Server/Client**,
**LangGraph** with sub-agents, **HuggingFace LLMs**, and
**Google Calendar API** — all wrapped in a glassy chat UI.

## Architecture